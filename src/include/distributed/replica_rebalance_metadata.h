#ifndef REPLICA_REBALANCE_METADATA_H
#define REPLICA_REBALANCE_METADATA_H

#include "postgres.h"
#include "fmgr.h"

// No specific function declarations needed here if UDF is only defined in .c
// and PG_FUNCTION_INFO_V1 is used there.
// This header is mostly for completeness or if other C files were to call these.

#endif /* REPLICA_REBALANCE_METADATA_H */
